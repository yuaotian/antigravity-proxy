# 开发规范和规则

- 用户约束：不要生成总结性Markdown文档；不要生成测试脚本；不要编译/运行（用户自行操作）。交互提问必须通过 zhi 工具。
- 用户强调：禁止生成总结性 Markdown/README；禁止生成测试脚本/测试代码；禁止编译与运行（用户自行编译/运行）。
- 用户再次强调：禁止生成总结性 Markdown/README；禁止生成测试脚本/测试代码；禁止编译与运行（用户自行编译/运行）。
- 用户再次强调：不要生成总结性 Markdown 文档；不要生成测试脚本；不要编译或运行（用户自己编译/运行）。
- 用户再次强调：不要生成总结性 Markdown 文档；不要生成测试脚本；不要编译或运行（用户自己编译/运行）。
- 用户再次强调：不要生成总结性 Markdown 文档；不要生成测试脚本；不要编译或运行（用户自己编译/运行）。
- 用户再次强调：不要生成总结性 Markdown 文档；不要生成测试脚本；不要编译或运行（用户自己编译/运行）。
- 用户再次强调：不要生成总结性 Markdown 文档；不要生成测试脚本；不要编译或运行（用户自己编译/运行）。
